# Task Report: T7_1 — E2E Test Scenarios

**Status:** DEFERRED — requires live OpenShift cluster  
**Agent:** Testing_Agent  
**Phase:** 7

## Reason for Deferral
E2E tests require a live OpenShift cluster with `openshift-tests` in PATH and run via `hack/e2e.sh`. This cannot be executed in working-folder mode without cluster access. The test scenarios and expected assertions are fully documented in `tasks.md §4 T7_1` and the EP test plan.

## Required Scenarios (documented in tasks.md)
1. No `driverConfig` set → defaults confirmed on CSIDriver + DaemonSet
2. `secretRotation.type: None` → `requiresRepublish=false`, DaemonSet args disabled
3. `secretRotation.type: Custom`, 300s → `requiresRepublish=true`, `--rotation-poll-interval=5m0s`
4. Toggle None → Custom → rotation state reverts correctly
5. `tokenRequests.type: Managed` with audience → CSIDriver tokenRequests set
6. `tokenRequests.type: Managed` with empty audiences → tokenRequests cleared
7. Upgrade: pre-existing manual tokenRequests + no driverConfig → preserved
8. Upgrade: no pre-existing tokenRequests → defaults maintained, no rolling update
9. Multi-cloud: AWS + Azure audiences simultaneously → both present on CSIDriver

## To Execute
```bash
hack/e2e.sh   # requires: live OpenShift cluster + openshift-tests in $PATH
```
