# Task Report — T7_4

**Change:** sscsi-254
**Task:** T7_4 — E2E upgrade-preservation + no-`driverConfig` default-parity scenarios
**Assigned Agent:** Testing_Agent
**Phase:** Phase 7: E2E Scenarios (highest-risk e2e set per `plan.md` §7)
**Status:** APPROVED

## Objective

Implement the source EP's e2e upgrade scenarios: minimal CR with no existing tokenRequests (default parity); minimal CR with pre-existing manually-patched Azure WIF audiences (verify preservation, no spec-hash change, no delete+recreate); DaemonSet args unchanged; post-upgrade opt-in.

## OAPE Commands Executed

| Command | Args | Outcome |
|---|---|---|
| manual | — | Implemented directly, consistent with `T7_1`–`T7_3` (bash e2e scenarios, not a Go/OAPE-eligible artifact) |

## Files Changed

- `hack/e2e.sh` — added `get_csidriver_token_requests_raw`, `get_csidriver_uid`, `get_node_daemonset_args` helpers, plus `test_upgrade_no_driverconfig_default_parity`, `test_upgrade_preserves_manually_patched_tokenrequests`, `test_upgrade_daemonset_args_unchanged`, `test_upgrade_post_opt_in_to_managed` scenario functions. **Reordered** the execution block so these four calls run *before* `T7_3`'s WIF (`Managed`-audience) calls.

## Sequencing Resolution (carried over from T7_3)

`T7_3`'s task report flagged that `tokenRequests.type` is a one-way, CEL-enforced transition on the singleton `ClusterCSIDriver`, and that any `Unmanaged`-dependent scenario (exactly what this task needed) must run before `T7_3`'s `Managed`-audience block. This was resolved by:
1. Inserting this task's four new function calls immediately after `test_rotation_cleanup` and before `T7_3`'s WIF block in the bottom execution sequence.
2. Making `test_upgrade_post_opt_in_to_managed` (this task's own transition-to-`Managed` scenario) the last `Unmanaged`-dependent call in the entire script, positioned immediately before `T7_3`'s block.
3. Updating both blocks' comments in `hack/e2e.sh` to cross-reference this ordering constraint for future maintainers.

## Verification Technique

"No spec-hash change / no delete+recreate" is verified via `metadata.uid` stability across a manual live-object patch + a 30s reconcile-wait window, per this task's explicit acceptance criterion — UID is a stronger delete+recreate signal than `resourceVersion` (which also changes on ordinary in-place updates).

## Verification

| Check | Command | Result |
|---|---|---|
| Syntax | `bash -n hack/e2e.sh` | PASS |
| Static verify (sanity) | `make verify` | PASS |
| Unit tests (sanity) | `make test-unit` | PASS (no regression; no Go code touched) |
| Full e2e | `make test-e2e` | Not run — requires a live OpenShift cluster, consistent with `AGENTS.md`/`docs/testing-guidelines.md` |

## Code Eval Gate

`evals/code-generation_eval.yaml` still has the pre-existing unresolved merge-conflict markers (empty case list) flagged since `T1_1` — no automated cases could run. Manual rubric-based review performed instead. Refinement rounds: 0.

## Deviations

- Eval case list empty due to the pre-existing merge-conflict-marker schema defect (unchanged from all prior tasks) — proceeded with manual review.
- Full `make test-e2e` execution not possible without a live cluster (consistent with repo's own testing guidelines and prior tasks).
- Reordered (not just appended) `hack/e2e.sh`'s execution block relative to `T7_3`'s prior wiring, to resolve the cross-task sequencing constraint documented in `T7_3`'s report — this is a deliberate, intentional edit to code written in the prior task, not an accidental conflict.
