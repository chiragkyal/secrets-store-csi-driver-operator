# Task Report: T3_1 — Implement dynamic CSIDriver AssetFunc + tokenRequests logic

**Phase:** Phase 3 — Dynamic CSIDriver AssetFunc
**Status:** Approved
**Code changes:** None (already implemented)

## Verification

- `getRequiresRepublish` mirrors rotation enabled state via `getSecretRotationConfig`
- `getTokenRequests` implements full preservation matrix (7 paths)
- `NewDynamicCSIDriverAssetFunc` wraps namespaceAssetFunc, reads live CSIDriver and ClusterCSIDriver
- Returns unmutated manifest on ClusterCSIDriver NotFound
- `stringValue` helper for safe pointer deref
- FR-006, FR-010, FR-011, FR-013, FR-015, FR-017 satisfied

## Files

- `pkg/operator/csidriver_asset.go` (162 lines)
