# Task Report: T4_1+T4_2 — rotationArgsDaemonSetHook + RunOperator Wiring

**Status:** APPROVED (combined)  
**Agent:** OperatorController_Agent | OAPE: api-implement  
**Phase:** 4 | **Completed:** 2026-07-01

## Files Touched
- `pkg/operator/starter.go`:
  - Added imports: `appsv1`, `factory`
  - Added `rotationArgsDaemonSetHook(dynamicClient)` — DaemonSetHookFunc factory
  - Added `setArg(args, prefix, value)` — prefix-match arg replacement helper
  - **RunOperator wiring** (T4_2):
    - `replaceNamespaceFunc` → `enrichedCSIDriverAssetFunc` in ConditionalStaticResourcesController
    - `nil` → `[]factory.Informer{dynamicInformers.ForResource(clusterCSIDriverGVR).Informer()}`
    - Added `rotationArgsDaemonSetHook(dynamicClient)` as 2nd variadic DaemonSet hook

## Verification
- `CGO_ENABLED=1 go build ./...` — PASSED
- `CGO_ENABLED=1 go vet ./...` — PASSED
- `go test ./pkg/... ./cmd/...` — PASSED (4/4, no regressions)

## Deviation
DaemonSetHookFunc signature is `func(*opv1.OperatorSpec, *appsv1.DaemonSet) error` — first param `_` (OperatorSpec) ignored since spec is read via dynamicClient for consistency with other helpers.
