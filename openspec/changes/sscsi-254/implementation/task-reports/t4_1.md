# Task Report: T4_1 — Wire controllers + hooks in starter.go

**Phase:** Phase 4 — Controller Wiring
**Status:** Approved
**Code changes:** None (already implemented)

## Verification

- csidriver.yaml split into separate `SecretsStoreDynamicCSIDriverController` with `NewDynamicCSIDriverAssetFunc`
- `WithSecretRotationDaemonSetHook` registered alongside `WithCABundleDaemonSetHook`
- `clusterCSIDriverInformer` passed as optional informer for immediate DaemonSet re-sync
- CSIDriver lister from `kubeInformersForNamespaces.InformersFor("").Storage().V1().CSIDrivers().Lister()`
- No new informer factories — reuses existing `dynamicInformers` and `kubeInformersForNamespaces`
- Same `getOperatorSyncState` gating (Constitution Principle IV)
- CA bundle hook undisturbed (Constitution Principle VIII)

## Files

- `pkg/operator/starter.go` (245 lines)
