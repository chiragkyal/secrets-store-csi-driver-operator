# Task Implementation Report

**Change**: sscsi-254
**Task ID**: T3_1
**Task Title**: Dynamic `AssetFunc` for `csidriver.yaml`
**Phase**: Phase 3: Dynamic `CSIDriver` Object Generation (Rotation + WIF Fields)
**Assigned Agent**: ControllerLogic_Agent
**OAPE Command**: manual
**Approved**: 2026-07-09T13:00:00Z
**User approved by**: explicit "approved" response in chat

## Summary

Implemented `withSecretsStoreCSIDriverAsset(base, lister)` in a new file, `pkg/operator/csidriverasset.go` — an `AssetFunc` wrapper that special-cases `csidriver.yaml`, decoding it via the vendored `resourceread.ReadCSIDriverV1OrDie`, setting `spec.requiresRepublish` from `T2_1`'s resolved rotation config and `spec.tokenRequests` when the resolved configuration is `Managed`, then re-serializing via `sigs.k8s.io/yaml`. All other asset names pass through unchanged. Added 8 table-driven test cases with a hand-written fake lister. `make verify`/`make test-unit` pass after one formatting fix.

## OAPE Commands Executed

| Command | Args | Outcome |
|---------|------|---------|
| N/A (manual agent — implemented directly per task payload) | — | Success |

## Code Changes

### Files modified or created

| File | Change |
|------|--------|
| `pkg/operator/csidriverasset.go` | New. `withSecretsStoreCSIDriverAsset()`, `getClusterCSIDriverSpec()`, `toStorageTokenRequests()`. |
| `pkg/operator/csidriverasset_test.go` | New. 5 test functions, 8 cases; hand-written `fakeClusterCSIDriverLister`. |

### Git diff summary

```
pkg/operator/csidriverasset.go      | 86 +++++++++++++++++++++++
pkg/operator/csidriverasset_test.go | 184 +++++++++++++++++++++++++++++++++++
2 files changed, 270 insertions(+)
```

## Verification

| Check | Result | Notes |
|-------|--------|-------|
| Task acceptance criteria | PASSED | `requiresRepublish`/`tokenRequests` mapping traces to FR-003/FR-004/FR-011 |
| `go build ./pkg/operator/...` | PASSED | |
| `go vet ./pkg/operator/...` | PASSED | |
| `gofmt -s -l` | PASSED (after 1 fix) | Both new files initially failed formatting |
| `make verify` | PASSED | |
| `make test-unit` | PASSED | |

## Code Generation Eval Gate

**Eval results**: `eval-results/code-generation-T3_1.yaml`

| Metric | Value |
|--------|-------|
| Overall score | N/A |
| Cases pass | 0/0 |
| Refinement rounds | 1 |

### Cases

| Case ID | Score | Pass | Notes |
|---------|-------|------|-------|
| — | — | — | No cases matched (corrupted `evals/code-generation_eval.yaml`, same defect as prior tasks). |

### Eval-driven code fixes applied

- Refinement round 1: `gofmt -s -w` applied to both new files (not eval-driven, part of mandatory verification loop).

## Deviations

None. Scope matches the task payload exactly — live-object `tokenRequests` preservation for the Unmanaged case is intentionally deferred to `T3_2`.

## Links

- Design bundle: `implementation/design-bundle.md` (snapshot at approval time)
- Eval results: `eval-results/code-generation-T3_1.yaml`
