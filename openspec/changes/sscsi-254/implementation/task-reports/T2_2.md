# Task Report: T2_2 — Implement getTokenRequests Helper

**Status:** APPROVED  
**Agent:** OperatorController_Agent | OAPE: api-implement  
**Phase:** 2 | **Completed:** 2026-07-01

## Files Touched
- `pkg/operator/starter.go` — added imports, `csiDriverGVR`, `getTokenRequests`, `liveCSIDriverTokenRequests`

## Key Design Decisions
- `Audiences` is `*[]SecretsStoreTokenRequest` — nil audiences with Managed type returns nil (no tokenRequests)
- `ExpirationSeconds`: `int32` in opv1 → `*int64` in storagev1 (only set when > 0)
- `Audience` is `*string` in opv1 → `string` in storagev1 (nil → empty string)
- `csiDriverGVR` extracted as package-level var for test access

## Verification
- `CGO_ENABLED=1 go build ./...` — PASSED
- `CGO_ENABLED=1 go vet ./...` — PASSED
