# Task Implementation Report

**Change**: csi-secrets-store-rotation-and-wif
**Task ID**: T3_4
**Task Title**: Split `csidriver.yaml` into its own controller call
**Phase**: Phase 3: Dynamic CSIDriver Object
**Assigned Agent**: ControllerLogic_Agent
**Approved**: 2026-07-09T17:29:00Z
**User approved by**: user confirmation ("Approve")

## Summary

Removed `"csidriver.yaml"` from the shared 8-file `WithConditionalStaticResourcesController` call in `pkg/operator/starter.go` and registered a second, dedicated `WithConditionalStaticResourcesController("SecretsStoreDynamicCSIDriverController", ...)` call scoped to `["csidriver.yaml"]`, using T3_2's `NewDynamicCSIDriverAssetFunc`. Both calls gate identically on `getOperatorSyncState`. This completes Phase 3 (Dynamic CSIDriver Object) in full.

## Code Changes

### Files modified or created

| File | Change |
|------|--------|
| `pkg/operator/starter.go` | Removed `"csidriver.yaml"` from the shared file list (now 7 files); added a second `WithConditionalStaticResourcesController` call scoped to `["csidriver.yaml"]`, using `NewDynamicCSIDriverAssetFunc(replaceNamespaceFunc(operatorNamespace), clusterCSIDriverInformer.Lister(), providerName, kubeInformersForNamespaces.InformersFor("").Storage().V1().CSIDrivers().Lister(), providerName)`. |

### Git diff summary

```
 pkg/operator/starter.go | 31 ++++++++++++++++++++++++++++++- (30 insertions, 1 deletion)
```

## Verification

| Check | Result | Notes |
|-------|--------|-------|
| Task acceptance criteria (`RunOperator` compiles; both controller instances register successfully; traces to FR-009) | PASSED | Confirmed via `go build ./...` and a `grep` showing `csidriver.yaml` appears exactly once, in the new call. |
| `go build ./...` | PASSED | Whole-repo build. |
| `go vet ./...` | PASSED | |
| `gofmt -s -l` | PASSED | Clean. |
| `make verify` | PASSED | FIPS warning only, expected locally. |
| `make test-unit` | PASSED | `pkg/operator` ok (2.640s). |

## Deviations

None. Did not change `shouldCreateFn`/`shouldDeleteFn` gating semantics for the remaining 7 files, per this task's Non-goals — both calls gate identically on `getOperatorSyncState` (Constitution Principle IV). Diff kept minimal (30 insertions, 1 deletion) as instructed, given the regression risk of touching an existing, working controller registration.

## Links

- N/A (direct mode — no design bundle composed).
