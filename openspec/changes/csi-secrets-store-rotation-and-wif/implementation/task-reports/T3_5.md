# Task Implementation Report

**Change**: csi-secrets-store-rotation-and-wif
**Task ID**: T3_5
**Task Title**: RBAC relevance verification (discovery)
**Phase**: Phase 3: Dynamic CSIDriver Object
**Assigned Agent**: RBACSecurity_Agent
**Approved**: 2026-07-09T17:39:00Z
**User approved by**: user confirmation ("approve")

## Summary

Resolved this task's `Evidence: PARTIAL` caveat with real evidence: investigated the actual upstream `kubernetes-sigs/secrets-store-csi-driver` repository (accessible via `gh api`, contrary to the planning-time assumption that it was out of reach). **Conclusion: no RBAC change is needed for this feature.** The pre-existing `serviceaccounts/token: create` grant in `assets/rbac/secretproviderclasses_role.yaml` is a vestige of the pre-v1.6.0 rotation controller (superseded by the kubelet-native mechanism this feature relies on) and has no corresponding entry anywhere in the current upstream RBAC templates.

## Code Changes

### Files modified or created

| File | Change |
|------|--------|
| None | Discovery-only task; the finding is "no RBAC change needed," so no file was touched. |

### Git diff summary

```
(no diff — discovery task, no repo files changed)
```

## Verification

| Check | Result | Notes |
|-------|--------|-------|
| Task acceptance criteria (a documented finding recorded before Phase 4 completes) | PASSED | Finding: no RBAC change needed. Full evidence trail below. |
| `make` targets | N/A | No code changed; nothing to build/test for this task. |

## Findings (evidence)

| Evidence | Source |
|---|---|
| Current upstream `role.yaml` (main `secretproviderclasses-role` ClusterRole) | `charts/secrets-store-csi-driver/templates/role.yaml` — grants `events`, `pods`, `secretproviderclasses`, `secretproviderclasspodstatuses`, `csidrivers`. **No `secrets` and no `serviceaccounts/token` permissions.** |
| `secrets` CRUD permission in this operator's RBAC | Matches upstream's **separate, optional** `role-syncsecret.yaml` (secret-syncing feature) exactly — legitimate, unrelated to WIF. |
| `serviceaccounts/token: create` in this operator's RBAC | **No corresponding entry anywhere** in the current upstream chart. No `rbac-secretprovidertokenrequest.yaml` file exists in the current templates listing at all (confirmed via `gh api repos/kubernetes-sigs/secrets-store-csi-driver/contents/charts/secrets-store-csi-driver/templates`). |
| Why it's gone upstream | v1.6.0 release notes (from T1_1): rotation-controller replacement "removes the need for privileged RBAC permissions (listing pods, secrets, and creating service account tokens)... Rotation-specific RBAC resources (`rbac-secretproviderrotation.yaml`, `rbac-secretprovidertokenrequest.yaml`) have been removed." |
| Kubelet-driven mechanism confirmation | Kubernetes CSI Token Requests docs (from T1_1): kubelet mints the token and passes it via `NodePublishVolume`'s `volume_context` — the driver never calls the TokenRequest API itself for this mechanism. |

## Deviations

None from the task's stated objective. Per this task's Non-goals ("do not add or remove RBAC without a documented finding"), the finding supports **no change** — removing the vestigial `serviceaccounts/token` permission is flagged as a suggested, separate follow-up cleanup rather than bundled into this rotation/WIF feature, to keep this change's scope and risk surface tight.

## Links

- N/A (direct mode — no design bundle composed).
