# Task Implementation Report

**Change**: csi-secrets-store-rotation-and-wif
**Task ID**: T5_3
**Task Title**: E2E: upgrade preservation
**Phase**: Phase 5: E2E Coverage
**Assigned Agent**: Testing_Agent
**Approved**: 2026-07-09T19:05:00Z
**User approved by**: user confirmation ("Approve")

## Summary

Added a "MANUAL VERIFICATION RUNBOOK: Upgrade preservation (SC-005)" comment block to `hack/e2e.sh`, documenting the exact `oc get`/`oc patch` commands needed to manually verify, on a real cluster, that a pre-existing manually-configured `CSIDriver.spec.tokenRequests` and rotation cadence survive an operator upgrade to a version that includes this feature, when `driverConfig.secretsStore` is left unset (SC-005, FR-003/FR-005/FR-009/FR-010/FR-012).

This task's own Implementation notes explicitly anticipated the possibility that no upgrade-testing mechanism exists in this repo's CI, and pre-authorized narrowing the acceptance criterion to a documented manual runbook in that case, provided the narrowing is flagged explicitly rather than silently dropping coverage. I verified there is no such mechanism anywhere in this repo (no "upgrade" references in any repo YAML, no upgrade Prow-job scaffolding, no operator-version-swap script) — consistent with `repo-assessment.md`'s own finding — and applied that narrowing, flagging it in the task presentation.

## Code Changes

### Files modified or created

| File | Change |
|------|--------|
| `hack/e2e.sh` | Added a documentation-only comment block (not wired into the executable test sequence, since it cannot be automated by a single-version script) covering: (1) pre-upgrade manual `CSIDriver.spec.tokenRequests` configuration and rotation-cadence recording, (2) the operator upgrade step (via normal OLM channel upgrade, outside this script's control), (3) post-upgrade verification that both are unchanged, and (4) cross-reference to the existing unit-level guarantees (`TestDefaultPathMatchesPreFeatureBaseline` from T4_3, tokenRequests preservation matrix from T3_3) that this runbook confirms at the cluster level. |

### Git diff summary

```
 hack/e2e.sh | +47 lines (comment-only runbook block)
```

## Verification

| Check | Result | Notes |
|-------|--------|-------|
| Shell syntax check | PASSED | `bash -n hack/e2e.sh` (comment-only addition) |
| Repo still builds | PASSED | `go build -mod=vendor ./...` |
| Actual upgrade test | N/A | Not automatable in this repo/task's scope — see Deviations |

## Deviations

Acceptance criterion narrowed from "an automated `hack/e2e.sh` scenario" to "an explicitly-documented manual verification runbook step" — both of which are listed as acceptable outcomes in this task's own Acceptance criteria ("Either an automated `hack/e2e.sh` scenario, or an explicitly-documented manual verification runbook step..."). This narrowing was pre-authorized by the task's Implementation notes for exactly this circumstance (no upgrade-testing mechanism found anywhere in this repo's CI setup) and is flagged here explicitly per those same notes, rather than silently dropping SC-005 coverage. The underlying byte-for-byte preservation guarantee remains fully covered at the unit level (T3_3, T4_3); this runbook is the real-cluster confirmation layer, not a replacement for that unit coverage.

## Links

- N/A (direct mode — no design bundle composed).
