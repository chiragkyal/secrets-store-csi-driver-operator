# Task Implementation Report

**Change**: csi-secrets-store-rotation-and-wif
**Task ID**: T2_5
**Task Title**: Wire rotation hook into `starter.go`
**Phase**: Phase 2: Secret Rotation DaemonSet Hook
**Assigned Agent**: ControllerLogic_Agent
**Approved**: 2026-07-09T16:43:00Z
**User approved by**: user confirmation ("approve")

## Summary

Registered `WithSecretRotationDaemonSetHook` as an additional `optionalDaemonSetHooks` argument to the existing `WithCSIDriverNodeService(...)` call in `pkg/operator/starter.go`, alongside (not replacing) `WithCABundleDaemonSetHook`. Added `clusterCSIDriverInformer := dynamicInformers.ForResource(gvr)` and passed its `.Informer()` into the previously-`nil` `optionalInformers` slice for event-driven resync, and its `.Lister()` into the new hook. This completes Phase 2 (Secret Rotation DaemonSet Hook) in full.

## Code Changes

### Files modified or created

| File | Change |
|------|--------|
| `pkg/operator/starter.go` | Added `factory` import; added `clusterCSIDriverInformer` variable (reusing the existing `dynamicInformers` factory for the `clustercsidrivers` GVR); changed `WithCSIDriverNodeService`'s `optionalInformers` argument from `nil` to `[]factory.Informer{clusterCSIDriverInformer.Informer()}`; added `WithSecretRotationDaemonSetHook(clusterCSIDriverInformer.Lister(), providerName)` as an additional hook argument. |

### Git diff summary

```
 pkg/operator/starter.go | 16 +++++++++++++++-  (1 file changed, 15 insertions, 1 deletion)
```

## Verification

| Check | Result | Notes |
|-------|--------|-------|
| Task acceptance criteria (`RunOperator` compiles with both hooks registered; `WithCABundleDaemonSetHook` remains present) | PASSED | Confirmed via `go build ./...` and a `grep` showing both hooks present. |
| `go build ./...` | PASSED | Whole-repo build. |
| `go vet ./...` | PASSED | |
| `gofmt -s -l` | PASSED | Clean. |
| `make verify` | PASSED | FIPS warning only, expected locally. |
| `make test-unit` | PASSED | `pkg/operator` ok (2.197s). |

## Deviations

None. Did not touch `WithConditionalStaticResourcesController`, per this task's Non-goals — that remains Phase 3's scope. Documented informer decision (event-driven, via the existing `dynamicInformers` factory) directly in code comments in `starter.go`, per this task's instruction to "document the choice."

## Links

- N/A (direct mode — no design bundle composed).
