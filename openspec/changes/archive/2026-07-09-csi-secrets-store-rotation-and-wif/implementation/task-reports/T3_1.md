# Task Implementation Report

**Change**: csi-secrets-store-rotation-and-wif
**Task ID**: T3_1
**Task Title**: `tokenRequests`/`requiresRepublish` extraction (nil-path preservation matrix)
**Phase**: Phase 3: Dynamic CSIDriver Object
**Assigned Agent**: ControllerLogic_Agent
**Approved**: 2026-07-09T16:53:00Z
**User approved by**: user confirmation ("approve")

## Summary

Implemented `getRequiresRepublish` and `getTokenRequests` in a new `pkg/operator/csidriver_asset.go`. `getRequiresRepublish` reuses T2_1's `getSecretRotationConfig` rather than duplicating its nil-path logic, guaranteeing the rotation-enabled flag and `requiresRepublish` can never disagree. `getTokenRequests` implements the full six-way preservation matrix from `openspec/inputs/ep.md`'s Test Plan, relying on the upstream `Audiences *[]T` pointer semantics to correctly distinguish "omitted" (preserve existing) from "explicitly empty" (clear all). Build, vet, `make verify`, and `make test-unit` all pass.

## Code Changes

### Files modified or created

| File | Change |
|------|--------|
| `pkg/operator/csidriver_asset.go` | New file. `getRequiresRepublish(driverConfig opv1.CSIDriverConfigSpec) *bool`, `getTokenRequests(driverConfig opv1.CSIDriverConfigSpec, existingTokenRequests []storagev1.TokenRequest) []storagev1.TokenRequest`, and a small `stringValue(*string) string` helper. |

### Git diff summary

```
 pkg/operator/csidriver_asset.go | 74 ++++++++++++++++++++++++++++++++++++++ (new file)
```

## Verification

| Check | Result | Notes |
|-------|--------|-------|
| Task acceptance criteria (traces to FR-004, FR-005, FR-006 read-only observation, FR-007; full six-way matrix covered) | PASSED | All six paths implemented and traced in code comments. |
| `go build ./pkg/operator/...` | PASSED | |
| `go vet ./pkg/operator/...` | PASSED | |
| `gofmt -s -l` | PASSED | Clean. |
| `make verify` | PASSED | FIPS warning only, expected locally. |
| `make test-unit` | PASSED | `pkg/operator` ok (2.203s); no new tests added in this task (deferred to T3_3), existing suite unaffected. |

## Deviations

None. Per this task's Non-goals: the "cannot revert from Managed" CEL immutability check (FR-006) was **not** re-implemented — this function only reads already-validated objects. Bounds validation (FR-008) was **not** implemented — upstream CRD concern. No `_test.go` changes in this task by design (T3_3 covers testing, mirroring the T2_1/T2_4 split from Phase 2).

## Links

- N/A (direct mode — no design bundle composed).
