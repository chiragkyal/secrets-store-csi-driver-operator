# Task Implementation Report

**Change**: csi-secrets-store-rotation-and-wif
**Task ID**: T3_2
**Task Title**: Dynamic `AssetFunc` for `csidriver.yaml`
**Phase**: Phase 3: Dynamic CSIDriver Object
**Assigned Agent**: ControllerLogic_Agent
**Approved**: 2026-07-09T17:05:00Z
**User approved by**: user confirmation ("approve")

## Summary

Implemented `NewDynamicCSIDriverAssetFunc` in `pkg/operator/csidriver_asset.go`: a `resourceapply.AssetFunc`-compatible factory that renders `assets/csidriver.yaml` with `spec.requiresRepublish`/`spec.tokenRequests` computed from the live `ClusterCSIDriver` and `CSIDriver` objects via T3_1's functions, then re-serializes via `sigs.k8s.io/yaml`. Resolved this task's `Evidence: PARTIAL` lister-mechanism question by reusing `kubeInformersForNamespaces.InformersFor("")` — already constructed cluster-scoped in `starter.go` — for the live `CSIDriver` lister, no new informer added. Verified the specific acceptance criterion (output parseable by `resourceread.ReadGenericWithUnstructured`) via a temporary standalone program, since it exercises the exact `StaticResourceController` dispatch path.

## Code Changes

### Files modified or created

| File | Change |
|------|--------|
| `pkg/operator/csidriver_asset.go` | Added `NewDynamicCSIDriverAssetFunc(namespaceAssetFunc resourceapply.AssetFunc, clusterCSIDriverLister cache.GenericLister, clusterCSIDriverName string, csiDriverLister storagelistersv1.CSIDriverLister, csiDriverName string) resourceapply.AssetFunc` and its new imports. |

### Git diff summary

```
 pkg/operator/csidriver_asset.go | +66 lines (NewDynamicCSIDriverAssetFunc + imports)
```

## Verification

| Check | Result | Notes |
|-------|--------|-------|
| Task acceptance criteria (traces to FR-004–FR-007; produces valid `storagev1.CSIDriver` YAML bytes parseable by `resourceread.ReadGenericWithUnstructured`) | PASSED | Confirmed via a temporary standalone program (not committed): marshaled output decoded to `*v1.CSIDriver`. |
| `go build ./pkg/operator/...` | PASSED | |
| `go vet ./pkg/operator/...` | PASSED | |
| `gofmt -s -l` | PASSED | Clean. |
| `make verify` | PASSED | FIPS warning only, expected locally. |
| `make test-unit` | PASSED | `pkg/operator` ok (2.163s). |

## Deviations

None. Per this task's Non-goals: `resourceapply.ApplyCSIDriver` was **not** modified; `assets/csidriver.yaml`'s static content was **not** modified — it remains the unmutated starting point read via `namespaceAssetFunc` on every call. No `_test.go` changes in this task by design (T3_3 covers testing, mirroring the Phase 2 T2_1–T2_4 split).

## Links

- N/A (direct mode — no design bundle composed).
