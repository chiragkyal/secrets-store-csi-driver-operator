# Task Implementation Report

**Change**: csi-secrets-store-rotation-and-wif
**Task ID**: T4_3
**Task Title**: Default-path regression test
**Phase**: Phase 4: Wire-up & Regression Guard
**Assigned Agent**: Testing_Agent
**Approved**: 2026-07-09T18:14:00Z
**User approved by**: user confirmation ("approve")

## Summary

Added `TestDefaultPathMatchesPreFeatureBaseline` to `pkg/operator/rotation_test.go`, a dedicated regression anchor asserting the rendered DaemonSet args are byte-for-byte identical to the documented pre-feature baseline (`repo-assessment.md` §3.2) for any cluster with `driverConfig.secretsStore` unconfigured. This test **caught a real defect**: `time.Duration.String()` renders exactly-2-minutes as `"2m0s"`, not the historical literal `"2m"` hardcoded in `assets/node.yaml`. Fixed in `pkg/operator/rotation.go` via a new `formatRotationInterval` helper. Per this task's Non-goals, production code was changed only because the regression test revealed an actual defect — not silently patched over by adjusting the test.

## Code Changes

### Files modified or created

| File | Change |
|------|--------|
| `pkg/operator/rotation.go` | **Bug fix**: added `formatRotationInterval(d time.Duration) string`, rendering whole-minute durations as `"Nm"` (e.g. `"2m"`) instead of Go's default `"Nm0s"` (e.g. `"2m0s"`). Wired into `WithSecretRotationDaemonSetHook` in place of the raw `interval.String()` call. |
| `pkg/operator/rotation_test.go` | Added `TestDefaultPathMatchesPreFeatureBaseline` (2 cases). Corrected 4 pre-existing test expectations (in `TestWithSecretRotationDaemonSetHook` and `TestCABundleAndRotationHooksCoexist`) that had encoded the buggy `"2m0s"`/`"5m0s"` values as if they were correct. |

### Git diff summary

```
 pkg/operator/rotation.go      | +15 lines (formatRotationInterval + call-site change)
 pkg/operator/rotation_test.go | +55 lines (new test), 4 lines changed (corrected expectations)
```

## Verification

| Check | Result | Notes |
|-------|--------|-------|
| Task acceptance criteria (test passes with the exact baseline values from repo-assessment.md §3.2) | PASSED | `TestDefaultPathMatchesPreFeatureBaseline`: 2/2 subtests, asserting literal `"--enable-secret-rotation=true"`/`"--rotation-poll-interval=2m"`. |
| Full `pkg/operator` suite (all 10 test functions) | PASSED | `go test ./pkg/operator/... -v` — all pass after the fix. |
| `go vet ./...` | PASSED | |
| `gofmt -s -l` | PASSED | Clean. |
| `make verify` | PASSED | FIPS warning only, expected locally. |
| `make test-unit` | PASSED | `pkg/operator` ok (2.313s). |

## Real Defect Found and Fixed

`time.Duration.String()` renders exactly-2-minutes (and exactly-5-minutes) as `"2m0s"`/`"5m0s"`, not the historical baseline literal `"2m"`/`"5m"`. Left unfixed, this would have caused an **unintended DaemonSet rollout for every existing cluster** the first time this feature's code ran against a `ClusterCSIDriver` with no `driverConfig.secretsStore` configured — a real regression against FR-003/FR-012 and `specs.md` SC-005 ("zero behavior change for unconfigured clusters"). This is exactly the class of bug this task exists to catch, and treated as release-blocking per its Implementation notes.

## Deviations

Production code was changed in this task. This is explicitly permitted by this task's Non-goals **only** when the regression test reveals an actual defect, which it did — documented above rather than silently worked around.

## Links

- N/A (direct mode — no design bundle composed).
